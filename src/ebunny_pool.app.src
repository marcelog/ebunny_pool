{application, ebunny_pool, [
  {description, "Generic RabbitMQ worker pool"},
  {vsn, git},
  {registered, [ebunny_pool_sup]},
  {applications, [
    kernel,
    stdlib,
    compiler,
    syntax_tools,
    lager,
    crypto,
    asn1,
    public_key,
    ssl
  ]},
  {mod, { ebunny_pool_app, []}},
  {env, []}
]}.
